                    <% if @homeroom.show_dibels? %>
                      <td class="dibels" href="<%= student_url(student_id, chart_start: "dibels") %>">
                          <%= presenter.performance_level %>
                      </td>
                    <% end %>
