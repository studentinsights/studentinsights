<hr>
<p>
  <h4>Somerville Public Schools - SPED referral packet</h4>
  <ul style="list-style-type:none">
    <li>Report date: <%= format_date(Date.today) %></li>
    <li>Generated by: <%= @current_educator.full_name %></li>
    <li>Source: <%= @url %></li>
  </ul>
</p>
<hr>
<p>
  <h4><%= "#{@student.first_name} #{@student.last_name}"%></h4>
  <ul style="list-style-type:none">
    <li>Birthdate: <%= format_date(@student.date_of_birth) %></li>
    <li>Language: <%= @student.home_language %></li>
    <li>Disability: <%= @student.disability || "None"%></li>
    <li>504 plan: <%= @student.plan_504 %></li>
  </ul>
</p>
<p>
  <h4>Current services:</h4>
  <ul style="list-style-type:none">
    <% @services.each do |service| %>
      <li><%= "Since #{format_date(service.date_started)} - #{service.service_type.name}" %></li>
    <% end %>
  </ul>
</p>
<p>
  <h4>Attendance record:</h4>
  <ul style="list-style-type:none">
    <li><%= "NOTE: #{@current_school_year} school year is still in progress" %></li>
    <% @student_school_years.each do |year| %>
      <li><%= "Year: #{year.name} - Absences: #{year.absences.count} - Tardies: #{year.tardies.count}" %></li>
    <% end %>
  </ul>
</p>
<p>
  <h4>Behavioral incidents:</h4>
  <% @discipline_incidents.each do |incident| %>
    <ul style="list-style-type:none">
      <li><%= "#{format_date(incident.occurred_at)} - #{incident.incident_code} - #{incident.incident_location}"%></li>
      <li><%= incident.incident_description %></li>
    </ul>
  <% end %>
</p>
<p>
  <h4>Academic assessments:</h4>
  <% @student_assessments.each do |test| %>
    <ul style="list-style-type:none">
      <li><%= "#{format_date(test.date_taken)} - #{test.assessment.family} #{test.assessment.subject} - #{test.percentile_rank.try(:ordinalize) || 'Unknown'} percentile" %></li>
    </ul>
  <% end %>
</p>
<p>
  <h4>Additional notes:</h4>
  <p>Coming Soon!</p>
</p>
